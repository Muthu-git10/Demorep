pipeline {
    agent { label 'agent-1' } // Target your Docker build agent node
    stages {
        stage('Test') {
            steps {
                git 'https://github.com/Muthu-git10/Demorep.git' // Change to your repo
                sh 'echo hello'
            }
        }
}
}
